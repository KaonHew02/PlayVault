/* PlayVault — games not built yet.

   Registered as stubs so the lobby is honest about the roster: listed, greyed,
   not clickable. Each one moves out of this file and into js/games/<code>/ when
   it is built — the lobby, statistics and save format need no change when it
   does.

   Empty is the goal. */
(function (PV) {
  'use strict';

  // Everything on the roster is built.

})(window.PV);
